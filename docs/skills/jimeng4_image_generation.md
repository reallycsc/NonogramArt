# 即梦4.0图片生成Skill

## 概述

使用即梦(Jimeng) CLI工具的4.0模型，批量生成画册配图，并生成对应的像素图。支持防爬限流、内容审核自动重试、进度断点续传。完整流程：原图生成 → 像素图生成 → 数据文件更新。

## 工具与依赖

| 依赖 | 说明 |
| ---- | ---- |
| `dreamina.exe` | 即梦CLI工具，位于项目根目录 |
| Python 3.11 | 运行生成脚本 |
| PIL/Pillow | 图片处理 |

## 提示词规则

### 提示词结构

```
{风格前缀}{场景描述}，{配色关键词}{风格后缀}
```

### 风格前缀（固定）

```
中国风卡通插画，Q版2-3头身比例人物，圆润流畅线条，色彩丰富多样，画面构图饱满，
```

### 配色关键词

**重要：不要使用固定配色关键词（如"赭石色水墨灰金色点缀"），这会导致所有图片变成同一色系。**

| 书架 | 配色关键词 |
| ---- | ---- |
| 人文历史 | 色彩搭配和谐 |
| 艺术创作 | 色彩搭配和谐 |
| 自然地理 | 色彩搭配和谐 |
| 生物世界 | 色彩搭配和谐 |
| 生活社会 | 色彩搭配和谐 |
| 科技工业 | 色彩搭配和谐 |
| 综合素材 | 色彩搭配和谐 |

> 经验教训：之前使用"赭石色水墨灰金色点缀"导致90%图片为暖红色调。去掉固定配色后，不同场景（森林、沙漠、海洋）能自然呈现不同色调。让场景描述本身决定色调，而非强制配色。

### 风格后缀（固定）

```
，画面边缘有丰富细节，不要大面积纯色背景，不要出现任何中文文字或汉字
```

### 负面提示词

```
写实照片,3D渲染,暗色调,灰暗,恐怖,血腥,真实人物,photorealistic,dark,gloomy,写实风格,摄影,中文文字,汉字,书法字,文字水印
```

### 完整提示词示例

| 场景 | 完整提示词 |
| ---- | ---- |
| 森林 | 中国风卡通插画，Q版2-3头身比例人物，圆润流畅线条，色彩丰富多样，画面构图饱满，远古人类在云南红土地上进行采集活动，手持简单石器，背景是茂密的原始森林和火山，色彩搭配和谐，画面边缘有丰富细节，不要大面积纯色背景，不要出现任何中文文字或汉字 |
| 沙漠 | 中国风卡通插画，Q版2-3头身比例人物，圆润流畅线条，色彩丰富多样，画面构图饱满，张骞出使西域，张骞带领使团骑骆驼西行，丝绸之路上的沙漠和绿洲，色彩搭配和谐，画面边缘有丰富细节，不要大面积纯色背景，不要出现任何中文文字或汉字 |
| 海洋 | 中国风卡通插画，Q版2-3头身比例人物，圆润流畅线条，色彩丰富多样，画面构图饱满，郑和站在宝船船头远眺，身后浩荡船队航行在大海上，色彩搭配和谐，画面边缘有丰富细节，不要大面积纯色背景，不要出现任何中文文字或汉字 |

## 生成参数

| 参数 | 值 | 说明 |
| ---- | -- | ---- |
| 模型 | 4.0 | 即梦4.0，VIP用户免费 |
| 宽高比 | 3:2 | 对应分块3×2 |
| 分辨率 | 2k | 生成2496×1664 |
| poll | 120 | 等待生成结果的最长时间（秒） |

### CLI命令格式

```bash
dreamina.exe text2image \
  --prompt="{完整提示词}" \
  --ratio=3:2 \
  --model_version=4.0 \
  --resolution_type=2k \
  --poll=120
```

### CLI返回格式

```json
{
  "submit_id": "uuid",
  "gen_status": "success",
  "result_json": {
    "images": [
      {
        "image_url": "https://...",
        "width": 2496,
        "height": 1664
      }
    ]
  }
}
```

### gen_status状态说明

| 状态 | 说明 | 处理方式 |
| ---- | ---- | ---- |
| success | 生成成功 | 下载图片 |
| fail | 生成失败 | 可能是内容审核，尝试安全提示词 |
| running/pending/queued | 排队中 | 用query_result轮询 |
| （无输出/JSON解析失败） | CLI异常 | 等待后重试 |

## 防爬限流策略

| 策略 | 参数 | 说明 |
| ---- | ---- | ---- |
| 请求间隔 | 15-35秒 | 随机延迟，模拟人工操作 |
| 批次休息 | 每10张休息60-120秒 | 防止短时间大量请求 |
| 重试延迟 | 30-60秒 | 失败后等待再重试 |
| 最大重试 | 3次 | 单次生成最多重试3次 |
| 安全提示词 | 2次 | 内容审核失败后替换敏感词重试 |

## 内容审核处理

### 敏感词替换表

| 原词 | 替换词 |
| ---- | ------ |
| 战争 | 故事 |
| 起义 | 事件 |
| 革命 | 变革 |
| 焚烧 | 处理 |
| 作战 | 行动 |
| 抗战 | 卫国 |
| 游行 | 集会 |
| 刺杀 | 会见 |
| 投江 | 远行 |
| 追击 | 追赶 |
| 冲锋 | 行军 |
| 激战 | 对峙 |
| 战火纷飞 | 风云变幻 |
| 攻占 | 进驻 |
| 行刺 | 拜见 |

### 审核处理流程

1. 首次使用原始提示词生成
2. 如果gen_status为fail，使用替换敏感词后的安全提示词重试
3. 最多尝试2次安全提示词替换

## 进度管理

### 进度文件

`tools/jimeng4_progress.json` 记录已完成、失败、被拦截的图片ID。

### 重新生成全部图片

```powershell
# 1. 删除所有旧图片
Remove-Item "assets\images\illustrations\{album_id}\chapter*.jpg"

# 2. 删除进度文件
Remove-Item "tools\jimeng4_progress.json"

# 3. 删除日志
Remove-Item "tools\jimeng4_generation.log"

# 4. 启动生成
C:\Python311\python.exe tools\generate_jimeng4_images.py generate
```

### 查看状态

```powershell
C:\Python311\python.exe tools\generate_jimeng4_images.py status
```

### 重试被拦截的图片

```powershell
C:\Python311\python.exe tools\generate_jimeng4_images.py retry_blocked
```

### 查看积分

```powershell
C:\Python311\python.exe tools\generate_jimeng4_images.py credit
```

## 提示词经验教训

### 已解决的问题

| 问题 | 原因 | 解决方案 |
| ---- | ---- | ---- |
| 所有图片红色主色调 | "暖色调为主"+"赭石色水墨灰金色点缀"强制暖色 | 改为"色彩丰富多样"+"色彩搭配和谐"，让场景决定色调 |
| 图片出现中文文字 | 提示词未限制文字 | 后缀加"不要出现任何中文文字或汉字"，负面提示词加"中文文字,汉字,书法字,文字水印" |
| 内容审核失败 | 历史场景含战争等敏感词 | 自动替换敏感词重试 |
| CLI输出含SQL日志 | 即梦CLI偶发输出调试信息 | JSON解析失败时重试 |

### 提示词迭代历史

| 版本 | 风格前缀 | 配色关键词 | 风格后缀 | 问题 |
| ---- | ---- | ---- | ---- | ---- |
| v1 | ...色彩鲜艳明亮饱和，暖色调为主... | 赭石色水墨灰金色点缀 | ...不要大面积纯色背景 | 90%图片红色主色调 |
| v2 | ...色彩丰富明亮，色调温暖... | 赭石色与水墨灰为点缀，金色细节装饰 | ...不要大面积纯色背景 | 仍偏红色 |
| v3 | ...色彩丰富多样... | 色彩搭配和谐 | ...不要大面积纯色背景 | 色调多样但出现文字 |
| **v4(当前)** | ...色彩丰富多样... | 色彩搭配和谐 | ...不要大面积纯色背景，不要出现任何中文文字或汉字 | ✅ |

## 图片规格

| 属性 | 值 |
| ---- | -- |
| 宽高比 | 3:2 |
| 分辨率 | 2496×1664 |
| 分块方式 | 3×2 = 6块/图 |
| 文件格式 | JPG（quality=95） |
| 文件命名 | chapter{N}_{序号}_{主题}.jpg |
| 平均大小 | 约1.2MB/张（JPG比PNG节省约75%空间） |

## 像素图生成

原图生成完成后，需要为每张图片生成对应的像素图版本，作为数织关卡完成后的填充模板使用。

### 像素图规则

| 规则 | 说明 |
| ---- | ---- |
| 命名规范 | 像素图文件名 = 原图文件名 + `_pixel.jpg` |
| 输出目录 | 与原图在同一目录，不单独创建子目录 |
| 分辨率 | 与原图相同分辨率（2496×1664） |
| 生成方式 | 根据数织关卡的网格数目生成对应的像素数量，不使用solution |
| 放大方式 | 使用 NEAREST 插值保持像素风格 |
| 文件格式 | JPG（quality=95） |

### 像素图尺寸计算

根据图片的分块配置（3×2）和每块的网格大小（N×N）计算小像素图尺寸：

| grid_size | 难度 | 小像素图尺寸 | 放大后尺寸 |
| --------- | ---- | ------------ | ---------- |
| 5 | tutorial | 15×10 | 2496×1664 |
| 10 | easy | 30×20 | 2496×1664 |
| 15 | medium | 45×30 | 2496×1664 |
| 20 | hard | 60×40 | 2496×1664 |
| 25 | expert | 75×50 | 2496×1664 |

> 计算公式：小像素图宽度 = 3 × grid_size，小像素图高度 = 2 × grid_size

### 像素图生成流程

1. 打开原图，获取宽高（img_width × img_height）
2. 计算小像素图尺寸：pixel_w = 3 × grid_size，pixel_h = 2 × grid_size
3. 计算每个分块的采样区域：block_width = img_width / 3，block_height = img_height / 2
4. 对每个像素点，计算其在原图中的采样坐标：
   - sample_x = x_block × block_width + block_width × (x_pixel + 0.5) / grid_size
   - sample_y = y_block × block_height + block_height × (y_pixel + 0.5) / grid_size
5. 从原图采样该坐标的颜色，写入小像素图
6. 使用 NEAREST 插值将小像素图放大到原图分辨率
7. 保存为JPG格式（quality=95）

### 采样坐标说明

采样时使用 `(x_pixel + 0.5) / grid_size` 确保采样点位于每个网格单元的中心，而非边缘。这样采样的颜色最能代表该网格单元的视觉内容。

```
原图 (2496×1664)                    小像素图 (30×20, grid=10)
┌──────────┬──────────┬──────────┐   ┌────┬────┬────┐
│          │          │          │   │ 0,0│ 1,0│ 2,0│  ← 每格采样中心颜色
│  block0  │  block1  │  block2  │   ├────┼────┼────┤
│ (832×832)│ (832×832)│ (832×832)│   │ 0,1│ 1,1│ 2,1│
│          │          │          │   ├────┼────┼────┤
├──────────┼──────────┼──────────┤   │ 0,2│ 1,2│ 2,2│
│          │          │          │   └────┴────┴────┘
│  block3  │  block4  │  block5  │
│ (832×832)│ (832×832)│ (832×832)│   NEAREST放大
│          │          │          │   → 2496×1664
└──────────┴──────────┴──────────┘
```

### 生成命令

```powershell
# 生成所有像素图
C:\Python311\python.exe tools\generate_pixel_images.py pixel

# 验证关卡文件完整性
C:\Python311\python.exe tools\generate_pixel_images.py verify

# 更新数据文件（添加pixel_image字段）
C:\Python311\python.exe tools\generate_pixel_images.py update_json

# 执行全部（像素图+验证+更新数据）
C:\Python311\python.exe tools\generate_pixel_images.py all
```

### 数据文件更新

像素图生成后，需要在 `data/pictures/chinese_history.json` 中为每张图片添加 `pixel_image` 字段：

```json
{
  "id": "chapter1_01_yuanmou",
  "image": "res://assets/images/illustrations/chinese_history/chapter1_01_yuanmou.jpg",
  "pixel_image": "res://assets/images/illustrations/chinese_history/chapter1_01_yuanmou_pixel.jpg",
  ...
}
```

### 文件大小参考

| 类型 | 平均大小 | 总大小(105张) |
| ---- | -------- | ------------- |
| 原图(JPG) | 约1.1MB | 约112MB |
| 像素图(JPG) | 约0.3MB | 约30MB |

> 像素图因大面积同色块，JPG压缩率更高，平均仅约0.3MB/张。

## 账户信息

| 项目 | 值 |
| ---- | -- |
| VIP等级 | maestro |
| 积分查询 | `dreamina.exe user_credit` |
| 4.0模型费用 | VIP免费（不消耗积分） |
